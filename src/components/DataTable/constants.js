/**
 * Shared constants for DataTable components
 */

// How many skeleton rows to show when loading
export const SKELETON_ROW_COUNT = 10;

// Default column size constraints
export const MIN_DEFAULT_COL_WIDTH = 60;
export const MAX_DEFAULT_COL_WIDTH = 500;

// Sorting direction enum
export const SORTING_ENUM = Object.freeze({
  ASC: false,
  DESC: true,
});
